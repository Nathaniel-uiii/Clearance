{
  "name": "prime-api",
  "private": true,
  "description": "FastAPI app — uses the Python venv in this folder (.venv).",
  "scripts": {
    "api": ".venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev": ".venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": ".venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
